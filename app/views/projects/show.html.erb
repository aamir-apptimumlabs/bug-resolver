<div class="container mt-4">
  <div class="row justify-content-center">
    <div class="col-md-8">
      <h1><%= @project.name %></h1>
      <p><%= @project.description %></p>

  <%= render partial: "dashboards/#{current_user.role}", locals: { project: @project } %>

    </div>
  </div>
</div>
